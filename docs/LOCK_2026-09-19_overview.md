# Lock 2026-09-19 — overview stack

Companion note to the HUD platform lock.

## Accepted earlier the same day

- Plan XY eyeball lock (fig1 Cleal frame; cluster display shift for 80/156/55*).
- Heights / lintels on `site_ground_od` ground seating.
- Horizon terrain mesh + Bulford-style sky colours.
- Moonrise/moonset via astronomy-engine, then Most N ↔ Most S azimuth dial.

## Public cut

Published tree keeps viewer + vendor + locked poses + ground OD + terrain JS + lock docs. Workshop nudge HTML, plan embeds, bak files, and process HALT docs are excluded.

See `LOCK_2026-09-19_hud_platform.md` for the HUD snapshot filename and run instructions.
