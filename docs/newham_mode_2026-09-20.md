# Newham mode — final (2026-09-20)

## Enquiry
One **Newham** button (default **off** = Heel Stone / centre). On = rectangle + dual station rays (both long sides moon ↔, both short sides sun ↔).

## 91 corner — final target LOCKED
- **EN:** **412287.630 / 142174.136** (OSGB)
- Baked into `data/locked_poses.json` as `hole_e_m` / `hole_n_m` on stone 91
- Also `data/station91_hole_approved.json`
- Fallen megalith **pose** remains **412285.696 / 142173.451** (mesh unchanged)
- Page reads hole from poses; pink ring marks the hole when Newham is on

## Cache
`locked_poses.js?v=newham-final-91hole-20260920c`
